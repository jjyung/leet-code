# 199. Binary Tree Right Side View

## Problem

<span style="color: #f59e0b;"><strong>Medium</strong></span>

Given the `root` of a binary tree, imagine yourself standing on the right side of it, return the values of the nodes you can see ordered from top to bottom.

![Pasted 2026-05-13-13-36-48.png](./199.%20Binary%20Tree%20Right%20Side%20View-assets/Pasted%202026-05-13-13-36-48.png)

![Pasted 2026-05-13-13-36-48 1.png](./199.%20Binary%20Tree%20Right%20Side%20View-assets/Pasted%202026-05-13-13-36-48%201.png)

<https://leetcode.com/problems/binary-tree-right-side-view/description/?envType=study-plan-v2&envId=top-interview-150>

## Answer

使用 BFS 層序遍歷，每層只取最後一個節點值即可。

```java
import java.util.*;

class Solution {
    public List<Integer> rightSideView(TreeNode root) {
        List<Integer> result = new ArrayList<>();
        if (root == null) {
            return result;
        }

        Queue<TreeNode> queue = new LinkedList<>();
        queue.offer(root);

        while (!queue.isEmpty()) {
            int levelSize = queue.size();

            for (int i = 0; i < levelSize; i++) {
                TreeNode node = queue.poll();

                if (i == levelSize - 1) {
                    result.add(node.val);
                }

                if (node.left != null) queue.offer(node.left);
                if (node.right != null) queue.offer(node.right);
            }
        }

        return result;
    }
}
```

### 說明

1. 用 queue 逐層處理節點。
2. `levelSize` 表示當前層節點數。
3. 每層迴圈中，索引 `i == levelSize - 1` 的節點就是該層最右側可見節點。
4. 依序加入每層答案即可得到由上到下的右視圖。

時間複雜度：O(n)  
空間複雜度：O(w)，`w` 為樹的最大層寬。
