# 162. Find Peak Element

## Problem

<span style="color: #f59e0b;"><strong>Medium</strong></span>

A peak element is an element that is strictly greater than its neighbors.

Given a 0-indexed integer array nums, find a peak element, and return its index. If the array contains multiple peaks, return the index to any of the peaks.

You may imagine that nums[-1] = nums[n] = -∞. In other words, an element is always considered to be strictly greater than a neighbor that is outside the array.

You must write an algorithm that runs in O(log n) time.

### Example 1

```text
Input: nums = [1,2,3,1]
Output: 2
Explanation: 3 is a peak element and your function should return the index number 2.
```

### Example 2

```text
Input: nums = [1,2,1,3,5,6,4]
Output: 5
Explanation: Your function can return either index number 1 where the peak element is 2, or index number 5 where the peak element is 6.
```

### Constraints

- `1 <= nums.length <= 1000`
- `-2^31 <= nums[i] <= 2^31 - 1`
- `nums[i] != nums[i + 1]` for all valid `i`.

https://leetcode.com/problems/find-peak-element/description/?envType=study-plan-v2&envId=top-interview-150

## Answer

### 解題思路

把相鄰兩個元素 `nums[mid]` 與 `nums[mid + 1]` 視為目前搜尋區間的斜率：

1. 若 `nums[mid] < nums[mid + 1]`，右側正在上升，因此從 `mid + 1` 往右至少能找到一個峰值；將左邊界移至 `mid + 1`。
2. 否則右側正在下降，`mid` 本身或其左側至少有一個峰值；保留 `mid`，將右邊界移至 `mid`。
3. 每次都排除一半區間。當 `left == right` 時，該索引即為峰值。陣列長度為 `1` 時，迴圈不會執行，直接回傳索引 `0`。

### Java 程式碼

```java
class Solution {
	public int findPeakElement(int[] nums) {
		int left = 0;
		int right = nums.length - 1;

		// 搜尋閉區間 [left, right]，其中必定至少存在一個峰值
		while (left < right) {
			int mid = left + (right - left) / 2;

			if (nums[mid] < nums[mid + 1]) {
				// 右側上升，峰值必定位於 [mid + 1, right]
				left = mid + 1;
			} else {
				// 右側下降，mid 或左側區間必定存在峰值
				right = mid;
			}
		}

		return left;
	}
}
```

### 說明

- 題目保證相鄰元素不相等，因此每次比較都能明確判斷往上或往下的方向。
- `mid` 永遠小於 `right`，所以存取 `nums[mid + 1]` 不會越界。
- 選擇比較右鄰居可直接使用 `left < right` 作為迴圈條件，無須額外處理 `mid == 0` 時讀取 `nums[mid - 1]` 的越界問題；上升時設為 `left = mid + 1`、下降時設為 `right = mid`，兩種情況都會嚴格縮小搜尋區間。
- 設 `n` 為陣列 `nums` 的長度。每次迭代將搜尋區間縮小約一半，時間複雜度為 `O(log n)`，額外空間複雜度為 `O(1)`。