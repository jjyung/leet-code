# 200. Number of Islands

Given an `m x n` 2D binary grid `grid` which represents a map of `'1'`s (land) and `'0'`s (water), return *the number of islands*.

An **island** is surrounded by water and is formed by connecting adjacent lands horizontally or vertically. You may assume all four edges of the grid are all surrounded by water.



**Example 1:**

```
Input: grid = [
  ["1","1","1","1","0"],
# 200. Number of Islands

## Question

<span style="color: #f59e0b;"><strong>Medium</strong></span>

給定一個由 `'1'`（陸地）與 `'0'`（海水）組成的二維網格 `grid`，請回傳島嶼數量。

島嶼定義為上下左右相連的一群陸地，四周皆為海水包圍。

https://leetcode.com/problems/number-of-islands/

## Answer

做法是經典 Flood Fill：

1. 逐格掃描 `grid`。
2. 每遇到一格 `'1'`，代表找到一座新島，答案加一。
3. 立刻用 BFS 把該島所有相連的 `'1'` 都改成 `'0'`，避免重複計數。

在 Java 中用 BFS 可以避免深度遞迴在極端案例造成堆疊溢位。

```java
import java.util.ArrayDeque;
import java.util.Queue;

class Solution {
    public int numIslands(char[][] grid) {
        int m = grid.length;
        int n = grid[0].length;
        int islands = 0;

        int[][] dirs = {{1, 0}, {-1, 0}, {0, 1}, {0, -1}};

        for (int i = 0; i < m; i++) {
            for (int j = 0; j < n; j++) {
                if (grid[i][j] != '1') {
                    continue;
                }

                islands++;
                Queue<int[]> queue = new ArrayDeque<>();
                queue.offer(new int[] {i, j});
                grid[i][j] = '0';

                while (!queue.isEmpty()) {
                    int[] cur = queue.poll();
                    int r = cur[0];
                    int c = cur[1];

                    for (int[] d : dirs) {
                        int nr = r + d[0];
                        int nc = c + d[1];

                        if (nr < 0 || nr >= m || nc < 0 || nc >= n) {
                            continue;
                        }

                        if (grid[nr][nc] != '1') {
                            continue;
                        }

                        grid[nr][nc] = '0';
                        queue.offer(new int[] {nr, nc});
                    }
                }
            }
        }

        return islands;
    }
}
```

### 說明

1. 每個格子最多被走訪一次，時間複雜度為 `O(m * n)`。
2. 佇列最壞可能裝下大量陸地，空間複雜度為 `O(m * n)`。
3. 本題只計算上下左右連通，斜角不算同一座島。

- Max Area of Island

- Surrounded Regions

- Flood Fill

- Pacific Atlantic Water Flow

- Rotting Oranges

- Walls and Gates

---

# 建議記法

這題可以記成一句話：

> 掃全圖，遇到一塊新的陸地就加一，然後用 DFS/BFS 把整座島淹掉。

Java 面試我會優先寫 BFS，因為比較不怕 stack overflow；\
但練習概念時，DFS 版本最容易理解。