# 130. Surrounded Regions

## Question

<span style="color: #f59e0b;"><strong>Medium</strong></span>

給定一個 `m x n` 的字元矩陣 `board`，每格是 `'X'` 或 `'O'`。

若一個由 `'O'` 組成的區域沒有接觸到邊界，則該區域會被 `'X'` 完全包圍。請你把所有被包圍的區域原地改為 `'X'`。

https://leetcode.com/problems/surrounded-regions/

## Answer

這題關鍵是反向思考：

1. 真正「安全」的 `'O'` 一定能從邊界走到。
2. 先從四條邊出發，將可達的 `'O'` 標記成 `'#'`。
3. 全圖掃描：
4. 還是 `'O'` 的代表被包圍，改成 `'X'`。
5. `'#'` 代表安全格，改回 `'O'`。

這樣就不需要對每個區塊單獨判斷是否接觸邊界。

```java
import java.util.ArrayDeque;
import java.util.Queue;

class Solution {
    public void solve(char[][] board) {
        int m = board.length;
        int n = board[0].length;

        Queue<int[]> queue = new ArrayDeque<>();

        for (int i = 0; i < m; i++) {
            addIfO(board, queue, i, 0);
            addIfO(board, queue, i, n - 1);
        }

        for (int j = 0; j < n; j++) {
            addIfO(board, queue, 0, j);
            addIfO(board, queue, m - 1, j);
        }

        int[][] dirs = {{1, 0}, {-1, 0}, {0, 1}, {0, -1}};

        while (!queue.isEmpty()) {
            int[] cur = queue.poll();
            int r = cur[0];
            int c = cur[1];

            for (int[] d : dirs) {
                int nr = r + d[0];
                int nc = c + d[1];

                if (nr < 0 || nr >= m || nc < 0 || nc >= n) {
                    continue;
                }

                if (board[nr][nc] != 'O') {
                    continue;
                }

                board[nr][nc] = '#';
                queue.offer(new int[] {nr, nc});
            }
        }

        for (int i = 0; i < m; i++) {
            for (int j = 0; j < n; j++) {
                if (board[i][j] == 'O') {
                    board[i][j] = 'X';
                } else if (board[i][j] == '#') {
                    board[i][j] = 'O';
                }
            }
        }
    }

    private void addIfO(char[][] board, Queue<int[]> queue, int row, int col) {
        if (board[row][col] == 'O') {
            board[row][col] = '#';
            queue.offer(new int[] {row, col});
        }
    }
}
```

### 說明

1. 從邊界做 BFS，時間複雜度為 `O(m * n)`，每格最多進出一次。
2. 空間複雜度為 `O(m * n)`，最壞情況下佇列可能存到大量格子。
3. 先標記安全格再統一翻轉，是本題最穩定且好實作的策略。