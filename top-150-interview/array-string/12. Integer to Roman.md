# 12. Integer to Roman

## Question

<span style="color: #f59e0b;"><strong>Medium</strong></span>

Seven different symbols represent Roman numerals with the following values:

| **Symbol** | **Value** | 
|---|---|
| I | 1 | 
| V | 5 | 
| X | 10 | 
| L | 50 | 
| C | 100 | 
| D | 500 | 
| M | 1000 | 

Roman numerals are formed by appending the conversions of decimal place values from highest to lowest. Converting a decimal place value into a Roman numeral has the following rules:

- If the value does not start with 4 or 9, select the symbol of the maximal value that can be subtracted from the input, append that symbol to the result, subtract its value, and convert the remainder to a Roman numeral.

- If the value starts with 4 or 9 use the **subtractive form** representing one symbol subtracted from the following symbol, for example, 4 is 1 (`I`) less than 5 (`V`): `IV` and 9 is 1 (`I`) less than 10 (`X`): `IX`. Only the following subtractive forms are used: 4 (`IV`), 9 (`IX`), 40 (`XL`), 90 (`XC`), 400 (`CD`) and 900 (`CM`).

- Only powers of 10 (`I`, `X`, `C`, `M`) can be appended consecutively at most 3 times to represent multiples of 10. You cannot append 5 (`V`), 50 (`L`), or 500 (`D`) multiple times. If you need to append a symbol 4 times use the **subtractive form**.

Given an integer, convert it to a Roman numeral.

<https://leetcode.com/problems/integer-to-roman/?envType=study-plan-v2&envId=top-interview-150>

## Answer

要將整數轉換為羅馬數字，我們可以使用貪心演算法，從最大的羅馬數字符號開始，依次減去對應的值，並將符號新增到結果字串中。對於特殊的減法情況，我們可以預先定義好這些特殊的組合。

下面是詳細的 Java 實現：

```java
class Solution {
    public String intToRoman(int num) {
        // 定義所有的羅馬數字符號及其對應的值
        int[] values = {1000, 900, 500, 400, 100, 90, 50, 40, 10, 9, 5, 4, 1};
        String[] symbols = {"M", "CM", "D", "CD", "C", "XC", "L", "XL", "X", "IX", "V", "IV", "I"};

        StringBuilder sb = new StringBuilder();

        // 遍歷所有的符號，從最大的值開始
        for (int i = 0; i < values.length && num > 0; i++) {
            while (num >= values[i]) {
                num -= values[i];
                sb.append(symbols[i]);
            }
        }

        return sb.toString();
    }

    public static void main(String[] args) {
        Solution sol = new Solution();
        System.out.println(sol.intToRoman(3));    // 應輸出 "III"
        System.out.println(sol.intToRoman(4));    // 應輸出 "IV"
        System.out.println(sol.intToRoman(9));    // 應輸出 "IX"
        System.out.println(sol.intToRoman(58));   // 應輸出 "LVIII"
        System.out.println(sol.intToRoman(1994)); // 應輸出 "MCMXCIV"
    }
}
```

### 說明

1. **定義符號和值**：

   - `values` 陣列包含所有羅馬數字符號對應的值，從大到小排列。

   - `symbols` 陣列包含所有羅馬數字符號，與 `values` 陣列一一對應。

2. **構建結果字串**：

   - 使用 `StringBuilder` 來構建結果字串。

   - 遍歷 `values` 陣列，對於每一個值，儘可能多地減去這個值，並將對應的符號新增到結果字串中。

3. **返回結果**：

   - 遍歷完成後，返回構建的結果字串。

這個方法通過貪心演算法，從最大的符號開始處理，保證了每次都選擇儘可能大的符號來表示當前的數值，從而構建正確的羅馬數字表示。